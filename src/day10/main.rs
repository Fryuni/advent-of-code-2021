//! Binary for solving day 10 of Advent of Code 2021
#![deny(missing_docs)]

use std::error::Error;

static INPUT_DIR: include_dir::Dir =
    include_dir::include_dir!("$CARGO_MANIFEST_DIR/src/day10/input");

fn main() -> Result<(), Box<dyn Error + 'static>> {
    Ok(())
}