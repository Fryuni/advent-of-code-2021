//! Binary for solving day 4 of Advent of Code 2021
#![deny(missing_docs)]

use std::error::Error;

static INPUT_DIR: include_dir::Dir =
    include_dir::include_dir!("$CARGO_MANIFEST_DIR/src/day4/input");

fn main() -> Result<(), Box<dyn Error + 'static>> {
    Ok(())
}